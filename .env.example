# === MÔI TRƯỜNG ===
# Copy file này thành .env và điền giá trị

# Cổng server (Render/Railway tự set PORT)
PORT=3000

# Mật khẩu đăng nhập admin
ADMIN_PASSWORD=ntd942010

# Thư mục lưu file (để trống = dùng thư mục gốc project)
# Trên VPS có thể đặt thành: /data hoặc /app/data
DATA_DIR=
